<h1>Book Index Page</h1>

<% @books.each do |book| %>
 <div class="card">
    <div class="container">
      <p><strong>Title: <%= "#{book.title}"%>
      <p><strong>Author: <%= "#{book.author}"%>
      <p><strong>Average Rating: [empty for now]</p>
    </div>
  </div>
<% end %>